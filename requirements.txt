# Full local/Colab environment, pinned to the exact versions train.py used to
# produce the committed artifacts/model.joblib. These MUST match for the pickled
# model to load (numpy 2.x can't unpickle 1.26 arrays; sklearn pickles are
# version-sensitive). The serving image uses the leaner requirements-serve.txt.
numpy==1.26.4
pandas==2.3.3
scipy==1.17.1
scikit-learn==1.8.0
xgboost==3.2.0
imbalanced-learn==0.14.1
scikit-optimize==0.10.2
matplotlib==3.8.4
seaborn==0.13.2

# Serving layer (src/sepsis_icu/serve.py). Not needed for the notebooks, but
# kept here so `pip install -r requirements.txt` provisions the whole project.
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
joblib==1.5.3
