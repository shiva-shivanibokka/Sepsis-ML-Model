# Pinned to a mutually-compatible set (verified to coexist).
# scikit-optimize is the constraint: 0.10.2 supports scikit-learn <= 1.5,
# so the whole stack is pinned around that. Bump together, not individually.
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
xgboost==2.0.3
imbalanced-learn==0.12.3
scikit-optimize==0.10.2
matplotlib==3.8.4
seaborn==0.13.2

# Serving layer (src/sepsis_icu/serve.py). Not needed for the notebooks, but
# kept here so `pip install -r requirements.txt` provisions the whole project.
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
joblib==1.4.2
