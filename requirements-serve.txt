# Pinned serving lock — MUST match the versions that pickled artifacts/model.joblib,
# or joblib.load fails in the container (numpy 2.x can't unpickle arrays written by
# 1.26, sklearn pickles are version-sensitive, etc.). These are the exact versions
# train.py used. Regenerate the model and these pins together.
#
# The Dockerfile installs from this file and then `pip install --no-deps .`, so pip
# never re-resolves to newer releases. imbalanced-learn is required transitively:
# importing the sepsis_icu package runs __init__ -> features/models, which import it.
numpy==1.26.4
pandas==2.3.3
scipy==1.17.1
scikit-learn==1.8.0
xgboost==3.2.0
imbalanced-learn==0.14.1
joblib==1.5.3
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
