# Fly.io deployment config for the ICU sepsis classifier API.
# Deploy with:  fly launch --no-deploy   (first time, to create the app)
#               fly deploy
# See docs/deploy.md for the full walkthrough.
app = "sepsis-icu-classifier"   # globally-unique Fly app name
primary_region = "iad"      # US-East; change via `fly platform regions`

[build]
  dockerfile = "Dockerfile"

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = "stop"     # scale to zero when idle (free-tier friendly)
  auto_start_machines = true
  min_machines_running = 0

  [[http_service.checks]]
    method = "GET"
    path = "/health"
    interval = "30s"
    timeout = "5s"
    grace_period = "10s"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"          # headroom for the pandas + sklearn/xgboost import and
                            # model load (256MB risks OOM on cold start)
